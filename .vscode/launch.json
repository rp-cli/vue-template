{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "url": "http://localhost:8000/",
            "type": "pwa-chrome",
            "webRoot": "${workspaceFolder}"
        }
    ]
}